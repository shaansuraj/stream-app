/**
 * JwtPayload interface represents the payload structure
 * for the JWT tokens used in the application.
 */
export interface JwtPayload {
    userId: number; // The unique identifier of the user
  }